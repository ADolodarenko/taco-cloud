spring:
  application:
    name: taco-cloud
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:postgresql://localhost:5434/tacocloud
    username: tacouser
    password: tacouser
    hikari:
      connection-timeout: 20000
      minimum-idle: 5
      maximum-pool-size: 12
      idle-timeout: 300000
      max-lifetime: 1200000
      auto-commit: true
logging:
  level:
    root: debug
